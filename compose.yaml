name: cppner
services:
  app:
    build:
      context: .
      args:
        - PROJECT_NAME=${COMPOSE_PROJECT_NAME}
        - CMAKE_VERSION=${CMAKE_VERSION}
        - buildpath=${buildpath}
    environment:
      - PROJECT_NAME=${COMPOSE_PROJECT_NAME}
